# Analog_Clock

This project is a simple responsive analog clock built using HTML, CSS, and JavaScript. The clock dynamically updates the hour, minute, and second hands to reflect the current time, with numbers and markers automatically rendered for clarity.